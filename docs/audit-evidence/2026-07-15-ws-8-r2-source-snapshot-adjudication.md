# WS-8 R2 source-snapshot Deep-audit adjudication

Deep audit IDs: `a60a81a7` (logic), `bbae5277` (edge cases), `41cfeef8`
(security), `c307a255` (performance), and `841009f8` (concurrency). Each
declared panel completed with zero failed voices; `debug_authorized: false`.
Repeated findings are merged by root cause below without dropping a distinct
claim.

| finding | decision | action | verification |
|---|---|---|---|
| Root `git archive` trusting the dedicated-account checkout through `safe.directory` could parse lower-privilege repository configuration as root. | accepted | Removed every `safe.directory` override from the runner. The archive subprocess now drops supplementary groups, gid, and uid to the proof-bound dedicated account before Git starts. | `test_source_snapshot_runs_git_as_the_dedicated_account` asserts the privilege-drop contract and absence of `safe.directory`; local SAST rejects wildcard trust and shell execution. |
| Other pre-authorization Git identity calls could still run as foreign root and rely on the wrapper's `GIT_CONFIG_*` exception. | accepted | Routed `rev-parse` and clean-checkout status through `_run_repo_git`, which drops root to the canonical checkout owner before Git starts. | `test_root_git_identity_checks_drop_to_the_checkout_owner` asserts absolute Git argv, privilege drop, and scrubbed config environment. |
| The source-snapshot test pinned argv shape but did not prove the actual root/foreign-owner operation before consuming a nonce. | accepted | Non-executing authorization verification now materializes the exact proof-bound snapshot in a temporary root preflight and reports `source_snapshot_preflight: ok`; nonce reservation remains untouched until a later `--execute`. | `test_verification_mode_preflights_source_before_nonce_reservation` proves materialization occurs and reservation does not; the target command must pass this gate before R2 execution. |
| Checkout path, owner UID, and Git metadata were not checked together before privilege drop. | accepted | Require the account-proof checkout to resolve exactly to canonical `REPO_ROOT`, require repo root and `.git` ownership to equal the proof UID, reject symlinks/missing metadata, and require live account UID equality. | `test_source_snapshot_requires_the_proof_bound_checkout`, `test_source_snapshot_rejects_account_uid_drift`, and the real snapshot integration test pass. |
| Global/system Git config, replacement refs, or optional locks could change the commit-addressed archive. | accepted | The child environment disables global/system Git config, replacement objects, and optional locks while preserving only a fixed executable PATH and C locale. | `test_source_snapshot_runs_git_as_the_dedicated_account` asserts every hardened environment field; Semgrep local trust-boundary rules report zero findings. |
| A dash-leading `source_commit` could become a root Git option. | rejected | The cited boundary already rejects anything except exactly 40 lowercase hexadecimal characters before any subprocess call; after this revision Git also runs as non-root. | `materialize_source_snapshot` lines containing the 40-hex check plus authorization negative tests; focused and full suites pass. |
| Command-scoped `-c safe.directory` might be ignored, require quoting, or mismatch a symlinked path. | rejected | This became moot because the accepted design removes the override entirely. The earlier target reproduction proved `-c` worked as a diagnostic, but it is no longer the execution mechanism. Argv lists also preserve spaces without shell quoting. | Repository search finds no runner `safe.directory` invocation; proof-bound canonical checkout tests pass. |
| The new test would always fail because production allegedly omitted the scrubbed `env`. | rejected | The cited `subprocess.run` has always carried an explicit environment; the auditor saw a shortened hunk and inferred absence incorrectly. The revised implementation adds further fields and the test passes. | Focused test, full transport suite, and `git diff` inspection show the explicit `env` argument. |
| `check=False` hides execution errors. | rejected | The boundary intentionally maps any nonzero Git status or empty archive to one redaction-safe `PaidTransportError`; raising `CalledProcessError` would not improve terminal semantics and risks surfacing private stderr. | Existing unavailable-archive error path and non-executing preflight fail closed before nonce reservation. |
| The live-archive regression test is slower than a pure argv mock. | rejected | Real archive/extraction is intentional integration coverage and completes in roughly a tenth of a second locally; a pure mock would recreate the original false confidence. The separate preflight proves the target ownership boundary. | Focused snapshot tests complete under one second; all WS-8 and repository regressions remain green. |
| The runbook accidentally dropped the completed account-proof prerequisite and the protocol test dropped the WS-8 prefix invariant. | accepted | Restored both while retaining the stricter exact R2 identity assertion. | `test_loads_distinct_ws8_contract` and runbook line review. |
| R2 should dynamically generate a study ID or replace the WS-7 predecessor field with R1. | rejected | The study ID is intentionally frozen by preregistration and authorization; dynamic identity would break reproducibility. `predecessor` denotes the prior WS-7 research study, while the terminated R1 attempt is separately disclosed. | Exact R2 protocol assertion, protocol validator, authorization binding, and R1 evidence artifact. |
| R1 needs a new hardcoded denylist because prose alone cannot prevent replay. | rejected | Existing root-owned study termination state already forbids every same-study continuation, the account lock serializes attempts, nonce files reject replay, and terminal markers are single-assignment. A second denylist would duplicate and risk drifting from authoritative state. | `test_nonce_and_account_lock_reject_replay_or_concurrency`, `test_partial_attempt_cannot_resume`, and `test_terminal_markers_are_single_assignment_and_reservation_is_immutable`. |
| The R1 zero-turn record lacked redaction-safe traceability. | accepted | Added the bound source commit, R1 protocol digest, nonce digest, envelope-source digest, root terminal references, and exact finality-test names. Root-marker hashes remain an explicit Owner/admin read-only gate before R2 rather than being fabricated. | Read-only `shasum` of both root terminal markers must be captured before installing the R2 envelope; raw streams and credentials stay private. |
| Concurrent checkout mutation or replacement refs could silently change the authorized commit. | accepted | Disabled replacement objects, bound the canonical checkout and owner, retained exact clean-HEAD authorization, and added the pre-reservation materialization gate. Content-addressed object mutation either preserves the authorized digest or fails the archive; no model process exists during preflight. | Clean-source authorization tests, proof-bound checkout tests, preflight test, and `GIT_NO_REPLACE_OBJECTS=1` assertion. |
| The root archive timeout should kill an untrusted process group. | rejected | Git now runs as the isolated non-admin account, not root; built-in `--format=tar` is a single read-only Git operation with global/system config disabled. Expanding the process supervisor is not warranted for this bounded pre-model read. | Fixed 120-second timeout remains; capability still cannot start until preflight succeeds. |

## Post-fix Deep confirmation

Post-fix audit `cbadf611` completed every declared voice with zero failed
voices and `debug_authorized: false`. Its claims were adjudicated against the
full files and focused RED/GREEN tests:

| finding | decision | action | verification |
|---|---|---|---|
| `--execute` still materialized the source after nonce reservation, so the original failure class could consume R2 again. | accepted | Reordered the executing path to create its private root and materialize the exact snapshot before calling `reserve_authorization`; cleanup runs on either pre-lease failure. | `test_execute_source_failure_does_not_reserve_nonce` forces materialization failure and proves reservation is never called. |
| The privilege-drop test checked only a callable shape and not actual syscall order or no-op semantics. | accepted | Centralized `drop_to_account` and behavior-locked `setgroups([])` → `setgid` → `setuid`, same-identity no-op, same-UID/wrong-GID refusal, and foreign-nonroot refusal. | `test_drop_to_account_orders_calls_and_noops_only_for_matching_identity`. |
| A linked-worktree `.git` file could point to metadata outside the proof account boundary. | accepted | Resolve bounded `.git`/`commondir` pointers, reject symlinks, malformed/oversized pointers and unsafe modes, and require every metadata directory to be proof-UID-owned below the proof home. | `test_source_snapshot_rejects_git_metadata_outside_the_proof_home` plus real worktree archive test. |
| A proof account with GID 0 could retain a privileged group after UID drop. | accepted | Reject non-positive live UID or GID before constructing the Git child. | `test_source_snapshot_rejects_root_group_account`. |
| The hardened-environment claim was broader than its assertions. | accepted | Assertions now cover `HOME`, both Git config paths, `GIT_CONFIG_NOSYSTEM`, replacement refs, optional locks, system attributes, fixed PATH, and C locale. | `test_source_snapshot_runs_git_as_the_dedicated_account` and `test_root_git_identity_checks_drop_to_the_checkout_owner`. |
| Repository-local fsmonitor or hooks could spawn account-controlled descendants before timeout handling. | accepted | Override `core.fsmonitor=false` and `core.hooksPath=/dev/null` on both identity and archive reads; the subprocess is already unprivileged and cannot cross the root boundary. | Exact argv assertions plus Semgrep and the real archive integration test. |
| A non-executing preflight alone was insufficient because state could drift before `--execute`. | accepted | The executing process repeats schedule construction and snapshot materialization before the lease; it never trusts the earlier process result. | The execute failure test proves same-process ordering. |
| The test suite assumes an account-owned checkout and is not meaningful in a root-owned clone. | rejected | That assumption is the protocol contract, not incidental test state: the dedicated benchmark checkout must be owned by its ordinary account. Root-owned or group/world-writable checkout metadata now fails closed. | Proof-bound checkout, UID drift, unsafe GID, and real current-worktree tests cover the supported boundary. |
| `/usr/bin/git` is not portable to every platform. | rejected | WS-8 is intentionally a pinned macOS/Xcode Command Line Tools study, and the runbook/account proof require that platform. PATH lookup would weaken executable identity. | Protocol platform/toolchain contract and exact argv assertions. |
| `_drop_to_account` was allegedly undefined. | rejected | The audit received a diff view that omitted its existing definition; the final revision further centralizes it as `ws8.drop_to_account` and aliases that shared helper in the transport. | Import/compile succeeds and the syscall behavior test calls the exact production helper. |
| Authorization examples allegedly omit `tier`. | rejected | `_PAYLOAD_FIELDS` requires `tier`, validation checks it, and all four committed examples contain it; the audit's artifact excerpt was truncated. | Protocol/template tests and repository search for each example's literal tier. |
| `source_checkout_is_clean` needs a second duplicate privilege-drop test. | rejected | Both `rev-parse` and status call the same argument-parametric `_run_repo_git`; the existing test proves that helper's executable, environment, and pre-exec contract, while authorization separately tests dirty-checkout rejection. | `test_root_git_identity_checks_drop_to_the_checkout_owner` and `test_dirty_source_checkout_cannot_authorize`. |
| Every bounded Git read needs a new process-group supervisor. | rejected | No repository command runs as root after the accepted fix; external hook/fsmonitor vectors are disabled, the operation is read-only and time-bounded, and adding a second supervisor would not improve the privilege boundary. | Fixed 10/120-second timeouts, unprivileged pre-exec, argv hardening, and zero SAST findings. |

Integrated verdict: the first exact-path trust patch was rejected and replaced.
The accepted R2 revision removes root Git trust, binds privilege drop and Git
metadata to the installed account proof, and materializes the exact source in
the executing process before nonce consumption. Fresh zero-cost regression is
green. R2 execution remains blocked on read-only hashes of the R1 root terminal
markers, merge of this fix, and a fresh source/protocol-bound envelope.

## Final fix-confirmation Deep audit

Audit `ebec7047` completed all 6 declared voices with zero failed voices and
`debug_authorized: false`. Two voices returned no concrete defect; the remaining
claims were merged by root cause and adjudicated below.

| finding | decision | action | verification |
|---|---|---|---|
| Runtime-tree hashes, executable ownership/digests, and exact CLI versions were rechecked only after nonce reservation. | accepted | Added `verify_runtime_preflight`, covering both hosts and all three runtime gates, to non-executing validation and the executing path before private source creation/reservation; the live executor repeats it after reservation to catch later drift. | `test_runtime_preflight_rechecks_both_executables_roots_and_versions` and `test_execute_runtime_failure_does_not_materialize_or_reserve`. |
| Root-owned mode-0600 envelopes/proofs made the runbook's non-sudo validation commands impossible. | accepted | Document every validation-only command as `sudo /usr/bin/python3`; clarify that root read-only validation does not reserve until `--execute` is present. | Runbook sections 3-4 and authorization file-mode tests. |
| The authorization-path Git test asserted only that `preexec_fn` was callable, not the checkout owner's target UID/GID. | accepted | Patch the shared drop factory and assert its exact `(checkout_uid, checkout_gid)` arguments in the root identity test; retain the syscall-order test. | `test_root_git_identity_checks_drop_to_the_checkout_owner` and `test_drop_to_account_orders_calls_and_noops_only_for_matching_identity`. |
| Uncommitted `.git/info/attributes` could alter `git archive`, while tracked `.gitattributes` could apply `export-ignore`/`export-subst`. | accepted | Forbid local info attributes in every proof-bound Git directory and reject any tracked `.gitattributes` in the exact source tree before archive. | `test_source_snapshot_rejects_unbound_or_writable_git_metadata` covers both local and tracked cases. |
| The pre-reservation materializer did not verify all required treatment roots and hashes. | accepted | Before returning, require both full-rules file hashes, Claude plugin and skills tree hashes, Codex skills tree hash, and every scheduled task fixture. | `test_source_snapshot_rejects_treatment_digest_drift` plus exact-commit materialization tests. |
| Git alternate object stores could move object provenance outside the proof home. | accepted | Forbid `objects/info/alternates` in both worktree and common Git metadata; environment-provided alternates are absent from the fixed child environment. | Adversarial metadata test rejects an outside alternate before Git/archive/reservation. |
| `git archive` inherited an unspecified root working directory that the demoted macOS child might not read. | accepted | Set `cwd=REPO_ROOT` explicitly while retaining the exact `-C REPO_ROOT` binding. | Source-snapshot argv test now asserts `cwd`; real archive integration passes. |
| Authorization identity/cleanliness Git checks did not enforce linked-worktree `gitdir`/`commondir` proof-home policy. | accepted | Move the metadata resolver into `ws8.proof_bound_git_directories` and call it from both identity/status and archive paths. | Root identity and outside-home linked-worktree tests use the shared production boundary. |
| Group/world-writable checkout or Git metadata rejection lacked a behavioral assertion. | accepted | Add an unsafe-mode adversarial case to the shared metadata test; both Git entrypoints consume that helper. | `test_source_snapshot_rejects_unbound_or_writable_git_metadata`. |
| Duplicate Git argv/environment/metadata code could drift across identity and archive paths. | accepted | Centralize metadata proof, hardened argv, hardened environment, and privilege drop in `ws8`; transport delegates to those primitives. | Code inspection, compile, both exact argv/env tests, and full regressions. |
| Mock ordering tests alone cannot prove the live root/foreign-owner macOS boundary. | accepted | Keep adversarial unit/integration coverage and require the merged target checkout's root validation command to pass all runtime/source preflights before `--execute`; that machine-specific check remains an explicit execution gate, not a claimed local result. | Zero-cost target validation must emit `authorization-valid-not-reserved`, `source_snapshot_preflight: ok`, and 0 paid turns before R2 nonce consumption. |
| The only safe fix is to abandon `git archive` for per-blob extraction. | rejected | With unbound local attributes and alternates forbidden, tracked attributes forbidden, exact commit/checkout bound, required output hashes verified, unsafe tar members rejected, and Git running unprivileged, archive no longer has the cited uncontrolled inputs. A bespoke blob extractor would add a larger parser and mode/path surface. | Adversarial metadata, treatment digest, tar-member, exact-commit, privilege, and pre-reservation tests pass. |

Final integrated verdict: every concrete final-audit defect was either fixed and
behavior-locked or rejected with a narrower proven boundary. No Owner decision
is outstanding. Code publication may proceed; paid R2 execution still cannot
start until merge, target zero-cost validation, R1 terminal-marker hashes, and a
fresh exact-digest envelope are complete.

## Verification closeout

| claim | fresh evidence | status |
|---|---|---|
| WS-8 repair behavior | Six suites: protocol 10, run 15, power 11, analyze 10, execute 22, transport 31; total 99/99 passed. | DONE |
| Hidden-scorer trust | `python3 benchmarks/aqg-workflow/selftest.py --sandboxed` → 13/13 task instruments trustworthy. | DONE |
| Repository regression | `python3 -m pytest -q tests` → 2770 passed, 1 skipped in 38.70s. | DONE |
| Static/local gates | compileall and `git diff --check` exit 0; Semgrep 3 custom rules over 3 implementation files reports 0 findings; construction check passes at plan tier over 12 files. | DONE |
| External audit | Seven Deep audit runs are cited above; final adjudication validates as 41 rows, 26 accepted, 15 rejected, 0 needs-user-decision. | DONE |
| Durable evidence | R1 redaction-safe termination note, R2 protocol/runbook/templates, this adjudication, and the construction ledger are updated. | DONE |
| Sensitive/paid boundaries | No benchmark model turn, credential read, raw-stream read, target root-state mutation, production write, deploy, restart, or branch-protection change occurred during repair. | DONE |
| Execution readiness | Merge, dedicated-checkout update, read-only R1 root-marker hashes, fresh envelope creation/install, and target root validation remain required before any R2 paid call. | PARTIAL |
