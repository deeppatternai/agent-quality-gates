# WS-7 B-port final deep-audit adjudication

Audits: `0173b086` (seven-voice deep audit), `0fc469b3` (review-input adequacy check), `2f7aee91` (source-excerpt deep audit), `07508afb` (five-voice source audit), and `11da65c2` (seven-voice repair audit)

| finding | decision | action | verification |
|---|---|---|---|
| A proven denied `~` file access was classified as a hard workspace failure before denial evidence was considered. | accepted | Moved the denied branch ahead of the home-shorthand failure in `workspace_check`; a home shorthand is never treated as a relative child. | `test_run_cell_keeps_a_permission_denied_home_read_as_redacted_diagnostic`; focused suite reports `221 passed`. |
| A malformed stream that also had a denied boundary attempt could raise from redaction and skip normal accounted-infrastructure handling. | accepted | Do not invoke redaction for parse-error streams; the redactor itself returns an empty invalid stream rather than raising. | `test_denied_boundary_with_malformed_stream_is_an_accounted_infra_cell_not_a_runtime_exception`; focused suite reports `221 passed`. |
| Permission denial matching was too narrow and the required B smoke was not bound to primary preflight. | accepted | Recognize explicit not-granted permission wording, add the separately authorized wrapped `--denial-smoke` command, and require its current redaction-safe receipt before primary reservation. | `test_permission_request_not_granted_phrase_is_a_proven_denial`, `test_denial_smoke_receipt_binds_the_redacted_stream_to_the_protocol`, and no-execute smoke refusal pass. |
| Assistant text and terminal result text could retain an attempted host or relative escape path after a denied tool result. | accepted | Redact denied target references and host/traversal paths recursively across assistant, user, and terminal events; analyzer rejects residual text outside the required init cwd. | Home-denied regression asserts no absolute, home, or relative escape text remains; focused suite reports `221 passed`. |
| The protocol promised a redaction-safe denied-attempt count, but frozen analysis did not report it. | accepted | Aggregate and expose the validated count in every arm and manipulation diagnostic. | `test_denied_external_file_attempt_is_rederived_without_host_path_in_ledger` asserts both report locations. |
| One audit voice treated the audit request text as untrusted-artifact prompt injection. | rejected | The cited text was the tool invocation metadata supplied by the reviewer, not a repository artifact or executable/project data; no source line supports a repository security defect. | The rerun uses literal source excerpts only in audit `2f7aee91`. |
| One audit voice said the first rerun received only a repository pointer, not source content. | accepted | Do not count `0fc469b3` as a code-review pass; reran `2f7aee91` with literal changed source, tests, analyzer, and protocol excerpts. | `2f7aee91` returned source-backed findings and is adjudicated below. |
| A parse-error cell with blank raw evidence is not treated by the analyzer as bounded attrition. | rejected | The frozen protocol explicitly makes any stream parse error whole-study-invalidating. The runner writes and charges the completed cell before aborting with `stream_parse`; a later analyzer must report the aborted study inconclusive, not silently recover it. | `protocol.json` validity contract and `test_denied_boundary_with_malformed_stream_is_an_accounted_infra_cell_not_a_runtime_exception` preserve the accounted-cell and abort semantics. |
| Normal completed streams without a denied external tool path could retain host/traversal paths from assistant or result free text. | accepted | Sanitize every persistable parseable stream, not only denied-path streams; use a filesystem-path regex that excludes URL delimiters, then let the analyzer reject any residual path. | `test_run_cell_scrubs_untrusted_free_text_even_without_a_denied_file_attempt` passes and retains `https://example.com` while removing host/traversal paths. |
| A smoke receipt did not bind the recorded CLI version to the CLI that will run the primary. | accepted | Obtain the current wrapped CLI version before receipt validation and require exact equality. | `test_denial_smoke_receipt_rejects_cli_drift_and_symlink_alias` passes. |
| The raw-stream residual-path scan hid every tool input, including non-file path-bearing tools. | accepted | Scan all persisted tool inputs; only valid in-workspace paths and the denied sentinel remain after runner sanitization. | Focused runner/analyzer suite reports `226 passed`. |
| A smoke receipt could be produced by an arbitrary denied path rather than the fixed smoke probe. | accepted | Require `run_cell` to observe exactly one denied Read at the frozen probe before it can mark the smoke verified; bind the probe and exact count in the receipt. | `test_denial_smoke_requires_the_exact_frozen_probe_path` passes. |
| Receipt creation could leave a raw artifact after a later receipt write failure. | accepted | Track created artifacts and remove only artifacts created by this call if receipt persistence fails. | Receipt writer now uses exclusive creation plus cleanup; focused runner suite reports `226 passed`. |
| Receipt symlink validation followed the symlink before testing it. | accepted | Reject a symlink path before resolution in the smoke launcher, receipt writer, and receipt validator. | `test_denial_smoke_receipt_rejects_cli_drift_and_symlink_alias` passes. |
| Workspace checking could throw if parsed path and denial tuples became misaligned. | accepted | Treat a missing denial entry as unproven and return the normal fail-closed verdict. | `test_workspace_check_fails_closed_when_denial_alignment_is_missing` passes. |
| A denial phrase such as “do not have permission” was not recognized. | accepted | Extend the explicit-denial matcher without treating generic file errors as denial proof. | `test_does_not_have_permission_phrase_is_a_proven_denial` passes. |
| Normal absolute in-cell file-tool paths were sanitized in persisted raw evidence, then compared to the unsanitized logical ledger paths by the analyzer. | accepted | The analyzer now rederives and compares the redaction-safe workspace verdict and denied count, but intentionally does not compare raw file-path strings once they have been privacy-sanitized. The no-spend 800-cell primary-wire fixture now emits an absolute `Edit` path on every cell. | `test_primary_runner_ledger_is_accepted_by_the_frozen_analyzer` passes with 800 absolute-path fixture events; no paid runner was invoked. |
| A timeout discarded complete partial stream events, which could hide a visible isolation failure behind init-less infrastructure attrition. | accepted | Preserve timeout stdout for parsing; malformed trailing JSON remains a hard stream-parse abort, while a complete preceding init event participates in isolation/memory/workspace checks. | `test_timeout_preserves_a_complete_contamination_init_for_hard_checking` passes; `protocol.json` documents the timeout rule. |
| The mandatory denial smoke could spend money without being included in the B study's cumulative budget evidence. | accepted | Reserve the smoke's full `$0.25` cap inside the unchanged `$99.00` study cap, set the primary cap to `$98.75`, require the exact smoke reservation, and include it in marker, manifest, completion, and analyzer cumulative accounting. | Protocol, runner, analyzer, and budget-lock regressions pass; the primary remains unexecuted. |
| A redacted smoke stream proved a denial but could not replay that it was the exact fixed probe. | accepted | For the one public fixed smoke probe only, persist a non-reversible probe token; receipt creation and validation rederive exactly one matching `Read` without retaining the path. | `test_denial_smoke_receipt_requires_replayable_exact_probe_evidence` and receipt binding/drift tests pass. |
| A finished process-group leader could leave a descendant running; solution archive read also had a check-then-read symlink window. | accepted | Reap the process group even after a reaped leader and archive through `O_NOFOLLOW` plus regular-file `fstat`. | `test_model_runner_reaps_a_possible_descendant_after_leader_exit` and `test_archive_solution_refuses_a_symlink_without_following_it` pass. |
| Two obsolete redaction helpers were unused. | accepted | Removed the unused helpers after the recursive stream redactor superseded them. | Focused runner/analyzer/protocol suite passes. |
| The scorer's deliberately allow-default profile was characterized as the model-child isolation policy. | rejected | The model child is separately default-deny. The scorer's documented bounded scope is network and user-home isolation; changing it to default-deny would break its required Python runtime. | `MODEL_ISOLATION_RUNBOOK.md`, `_model_child_profile`, and existing sandbox regressions distinguish the two profiles. |
| The trusted scorer directory was said to be writable by untrusted solution code. | rejected | The trusted parent loads and unlinks `checks.py` before importing model code; denying that directory's writes would block this required one-way unlink, while the worker cannot recover the unlinked checks. | `score_subprocess.py:4-9`; hidden-check regression suite passes. |
| System-file read denial was required for the scorer profile. | rejected | No raw credential, user-home, or network access is granted to the scorer; the documented scorer boundary is not a general deny-default container. Expanding it would be a new environment contract, not a source-backed defect in this B port. | `PREREGISTRATION.md` states the scoped limitation; macOS scorer selftest passes. |
| The smoke receipt accepted a caller-supplied literal-path boolean while its durable proof used a redacted probe token; token counting also did not require denial for that exact Read. | accepted | Removed the caller boolean from receipt creation. The receipt now derives exactly one tokenized `Read` with matching explicit denial from its durable raw evidence, while `run_denial_smoke` separately requires the original literal probe before redaction. | Receipt regression asserts the token, exactly-one count, and aligned denial tuple; invalid un-tokenized raw still refuses. |
| The frozen analyzer could not rederive a redacted denied external attempt. | rejected | `workspace_check` already treats the denied sentinel/token as an explicitly denied out-of-boundary attempt. The existing analyzer regression passes a redacted raw stream through `analyze_records`; it was strengthened to call `_verify_raw_artifacts` directly. | `test_denied_external_file_attempt_is_rederived_without_host_path_in_ledger` passes. |
| Privacy-redacted normal in-workspace paths are not compared byte-for-byte with the ledger's logical path list. | rejected | This is the documented privacy boundary: a raw absolute in-cell path is deliberately replaced before persistence, so comparing it to the logical path list is impossible without retaining host paths. The analyzer verifies the rederived workspace verdict/count instead; the field is not an analysis input. | `PREREGISTRATION.md` documents the boundary and the 800-cell primary-wire test verifies it. |
| The scorer could signal a recycled process-group identifier after its successful leader had already been reaped. | accepted | Removed success-path group signalling. The trusted scorer now launches each untrusted solution call through a separate default-deny, no-fork worker profile, so a successful worker cannot leave a detached model-created child. Timeout paths still kill and bounded-reap their live process groups. | macOS nested-worker fork regression, scorer selftest, and focused suite pass. |
| Timeout cleanup could block without a bound or fail to preserve the partial model stream. | accepted | Both model and scorer timeout paths now use a bounded reap; the model runner rethrows the original partial stdout so `run_cell` can perform its hard isolation checks. | `test_model_runner_preserves_partial_timeout_evidence_after_reaping` passes. |
| Invalid negative/non-finite reported cost could be serialized into a cell while frozen analysis rejected the record. | accepted | Invalid reported cost is now persisted as JSON-safe `null` with `reserved_invalid` accounting and the conservative reservation. | Invalid-cost regression asserts `cost_usd is None`; focused analyzer suite passes. |
| The smoke receipt did not bind its raw cost/accounting evidence. | accepted | Receipt creation/replay now derives, records, and validates reported cost, accounted cost, accounting mode, and the frozen diagnostic reservation; invalid and over-cap costs refuse receipt creation. | Receipt-binding regression asserts the recorded fields; focused suite passes. |
| Solution archiving read unbounded model output into memory and could leave a partial exclusive destination on failure. | accepted | Archive reads in bounded chunks, refuses oversized files, and removes a destination it created unless copy completion succeeds. | `test_archive_solution_bounds_size_and_removes_partial_archive` passes. |
| The scorer comment described a write grant even though the protected worker directory was write-denied. | accepted | Rewrote the comment and split trusted-parent versus untrusted-worker roles so the write-deny and hidden-check order are explicit. | Source review plus macOS scorer selftest pass. |
| The final review claimed the model timeout handler did not reap or rethrow. | rejected | The cited review artifact ended before the actual `communicate`/rethrow lines. The full source reaps after group kill and preserves the original timeout's output; a targeted regression now exercises that path. | `test_model_runner_preserves_partial_timeout_evidence_after_reaping` passes. |
| The scorer's per-call JSON proxy could change the current task-checker contract. | rejected | The ten frozen checkers use pure positional, JSON-compatible inputs and judge returned values/exceptions; the worker deliberately rejects unsupported future checker input instead of silently changing it. Every current checker executes through that proxy against both held-out references. | `selftest.py --sandboxed` reports all 10 good references clean and all 10 bad references caught. |
| The outer scorer timeout was classified as infrastructure even when it could be caused by model code across multiple worker calls. | accepted | Reclassified the outer watchdog as a model error while retaining explicit harness classification for invalid runner protocol/startup failures. | `test_sandboxed_scorer_outer_timeout_is_a_model_error` passes. |
| The required smoke had a one-time receipt path but no one-time diagnostic reservation, allowing alternative receipt paths to consume more than `$0.25`. | accepted | Added a checkout-local exclusive diagnostic-attempt marker before the smoke model turn; the receipt hash-binds that marker and primary preflight revalidates it. | `test_denial_smoke_reservation_is_single_use_per_checkout` and receipt replay tests pass. |
| A solution archive failure overwrote an observed pass/fail/error with `infra_failed`, losing the endpoint classification. | accepted | Preserve the observed score and record `evidence_durability_failed`; primary collection aborts and frozen analysis rejects incomplete durable evidence. | `test_archive_failure_preserves_the_observed_score_and_flags_durability` passes. |
| Scoring copied a model solution with an unbounded `copy2` before the bounded archive path ran. | accepted | The scorer now uses the same no-follow, regular-file, bounded chunk copy as the archive before starting any worker. | `test_sandboxed_scorer_rejects_oversized_solution_before_starting_worker` passes. |
| A timeout during bounded reaping could leave a late descendant without a second kill attempt. | accepted | Both model and scorer timeout handlers repeat the process-group kill if bounded reaping itself times out. | Existing timeout regressions plus the sandboxed selftest pass. |
| Frozen analysis rederived raw reported cost but did not bind conservative accounted cost or accounting mode. | accepted | Recompute both fields through `_accounted_cost` for every parseable stream and reject a ledger mismatch. | `test_raw_stream_rederives_accounted_cost_and_accounting_mode` passes. |
| A cursor mismatch in atomic schedule verification could consume the next event as a block receipt. | accepted | Stop schedule parsing at the first mismatched cell rather than consuming a later event under the wrong block. | `test_atomic_block_cursor_stops_at_a_mismatched_cell_before_its_receipt` passes. |
| The raw init `cwd` was characterized as publication leakage. | rejected | It remains only in local raw evidence so the analyzer can rederive the private cell boundary; the result ledger carries only a logical relative cell root, raw evidence never enters the result PR, and the path contains no user-home identity. | `PREREGISTRATION.md` result boundary plus `verify_result_boundary.py --path benchmarks/aqg-workflow` pass. |
| A primary test did not directly prove that a missing smoke receipt stops before later integrity commands. | accepted | Added a real `preflight_primary_environment` regression that reaches the receipt gate and proves integrity-command execution is not reached. It stubs CLI/auth capability only, never a paid model call. | `test_primary_preflight_refuses_missing_smoke_before_integrity_commands` passes. |

## Follow-up audit execution limitation

The completed source-backed audits above remain the adjudicated external review
record. Three later replacement panels (`6750c121`, `ca2decec`, and `ee5ca8bd`)
were stopped after 5–10 minutes with no terminal finding payload from the
external service; they are **not** counted as passing audits. The changes made
after `11da65c2` are instead bound to the focused regressions listed above,
the sandboxed ten-task selftest, the full repository suite, and the pre-commit
construction checker below.

## Test-quality review

The refreshed signal-only review found 187 added test functions: 164 behavioral-only, one mixed,
and no shape-only tests. Its 40 low-coverage candidates are a port-wide base-diff
artifact, covered by the focused runner/analyzer/protocol suites, the ten-instrument
selftest, and the supported suite. Seventeen suppression candidates are inherited
platform/environment gates; the fresh full suite had only one actual skip. Two deletion
candidates are base-diff artifacts from replacing B's old runner path; the repair did
not remove an assertion and the 800-cell primary-wire fixture covers the replacement.
Disposition: covered elsewhere; no weak test was accepted as evidence.
