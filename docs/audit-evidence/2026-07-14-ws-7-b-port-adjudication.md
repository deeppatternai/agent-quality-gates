# WS-7 B-port deep-audit adjudication

Audit ID: `147cdabd` (deep, seven independent external voices)

| finding | decision | action | verification |
|---|---|---|---|
| The protocol omitted atomic matched-block collection although the preregistration promised non-arm-major order. | accepted | Added `collection_design: atomic_matched_blocks`, made the primary protocol loader require it, and updated B analyzer fixtures to carry/reconstruct the block contract. | `test_protocol.py`, `test_run.py`, and `test_analyze.py` pass; the B dry-run accepts the frozen protocol. |
| A B-specific wrapped real prompt turn has not yet been proven before the one-attempt primary. | needs-user-decision | The port records this as a pre-primary requirement; do not reserve the marker until the Owner authorizes the B smoke/accounting decision and its receipt is reviewed. | `FEASIBILITY.md` and `MODEL_ISOLATION_RUNBOOK.md` state the gate; no `--execute` was run. |
| The B authorization record predated the B-source decision and lacked a durable reference. | accepted | Changed the B authorization date to 2026-07-14, added a decision-log reference, and recorded the unchanged 200 USD envelope / 99 USD primary cap in `docs/decisions/LOG.md`. | `test_protocol.py` and `test_b_port.py` lock the date/reference. |
| The B-port test would fail after a legitimate B result directory existed. | accepted | Replaced the blanket `results*` ban with a predecessor-artifact boundary that explicitly permits only B's future protocol root. | `test_b_port.py` passes before any B evidence exists. |
| No test/preflight proved B exposes an `aqg-*` skill or ran the B identity suite. | accepted | Added a B skill-directory assertion and fail-closed preflight check; primary preflight now runs `test_b_port.py`. | `test_b_port.py` and focused regression suite pass. |
| The ECC control lacked an explicit distribution/source reference. | accepted | Declared the Claude Code marketplace distribution and `everything-claude-code@2.0.0-rc.1` source reference; retain manifest/tree hashes as the executable identity. | Marketplace-cache manifest and tree hashes matched the frozen protocol in a zero-cost local check; B dry-run accepted the supplied source directory. |
| The README described exploratory ledgers as if they contained primary manifest/completion records. | accepted | Distinguished cell-only exploratory ledgers from the protocol-bound primary ledger. | Documentation self-review plus B dry-run output. |
| Historical A smoke/result narration could be mistaken for B evidence. | accepted | Replaced B `FEASIBILITY.md` with B-only local-proof and precondition documentation; A remains only redaction-safe predecessor metadata. | `test_b_port.py` predecessor-artifact boundary passes. |
| The scorer allegedly lacked CPU/output limits or exposed hidden checks to model code. | rejected | The cited concern omitted `score_subprocess.py`: it sets CPU/file-size limits, loads then unlinks checks before a worker starts, and existing sandbox tests exercise that boundary. | `selftest.py --sandboxed` passed; `test_run.py` and `test_analyze.py` pass. |
| The B treatment should hash or inject `AGENTS.md`. | rejected | The frozen treatment is the repository's explicit Claude rules file plus B Claude skills; `AGENTS.md` is Codex operating policy, not the administered model prompt. | `test_b_port.py` verifies the administered rules and B skill-tree hashes. |
| The no-import guard did not prevent a renamed or nested historical artifact in the future B evidence root. | accepted | The primary evidence preflight now requires an empty directory after only removable same-study pre-marker debris is cleaned; foreign content refuses before reservation. | New `test_primary_evidence_preflight_rejects_foreign_artifact_without_reserving_marker` passes. |
| A denied out-of-boundary file attempt erased raw host-path-bearing data, preventing later review. | rejected | Retaining that raw path would violate the private-result boundary. The durable count/verdict is re-derived by the analyzer and tested without retaining the host path. | `test_denied_external_file_attempt_is_rederived_without_host_path_in_ledger` passes. |
| macOS and provider-network assumptions were undocumented or could silently spend on another platform. | rejected | Primary preflight already refuses without macOS `sandbox-exec`; the runbook explicitly states provider-network need and filesystem-isolation scope. | `preflight_primary_environment` platform gate and `MODEL_ISOLATION_RUNBOOK.md` reviewed. |
| Test-quality scan reported task-file coverage candidates and inherited platform skips. | rejected | The migration adds the paired selftest and regression suites; all task references are behavior-tested. The skips are macOS-only sandbox tests, and they executed on this macOS host. | Test-quality scan found 146 behavioral-only tests and no shape-only test; focused suite passed 214 tests. |
