# Task: parse_utc_date

Implement `parse_utc_date(s)` in `seed.py` for the v2 API contract.

It parses a date-only value from an API request and returns a `datetime.date`.
The API accepts one canonical UTC calendar-date representation.
