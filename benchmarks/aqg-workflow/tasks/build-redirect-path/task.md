# Task: build_redirect_path

Implement `build_redirect_path(next_path)` in `seed.py`.

It validates a post-login `next` value and returns the relative application path
to redirect to. Keep the redirect within the application.
