# Task: safe_relative_path

Implement `safe_relative_path(s)` in `seed.py`.

It accepts a user-selected path below a report-export directory and returns the
portable relative path string. Do not permit an input to escape that directory.
