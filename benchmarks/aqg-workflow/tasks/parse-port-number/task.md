# Task: parse_port_number

Fix `parse_port_number(value)` in `seed.py`.

It parses a user-supplied TCP port value and returns an integer suitable for a
socket connection. Production accepted values that could not be valid TCP
ports. Decide the boundary contract and enforce it consistently.
