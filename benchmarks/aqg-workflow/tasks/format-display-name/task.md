# Task: format_display_name

Implement `format_display_name(given, family)` in `seed.py` for the new profile
card. It should render one compact human-readable label from the two name
fields. Treat absent names deliberately rather than leaving a visibly broken
label in the new UI.
