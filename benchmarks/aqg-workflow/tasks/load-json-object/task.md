# Task: load_json_object

Implement the new `load_json_object(s)` request-ingestion helper in `seed.py`.

It parses a JSON configuration payload from a request and returns the decoded
object that downstream configuration code can consume.
