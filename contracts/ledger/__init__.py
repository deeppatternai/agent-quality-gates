"""AQG Project Ledger — neutral contract (schema + conformance + project_id + paths).

Pure-stdlib so both the AQG inbox consumer and the EAF exporter can import it
with zero third-party dependencies (DesignSpec OQ-10: AQG ships to others, no
heavy deps). See contracts/ledger/README.md for the §7 handoff alignment.
"""
