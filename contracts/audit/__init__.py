"""AQG de_audit interface contract (contracts/audit/).

Ships the de_audit request/response event SHAPE + a non-implementing stub — NOT
the engine. Vendor routing, panel-convergence weighting, and ground_truth are
engine-internal (the external cloud decision-engine) and are intentionally absent.
See contracts/audit/README.md.
"""
