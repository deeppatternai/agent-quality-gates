interface:
  display_name: "AQG Multi-Review"
  short_description: "5-dimension code review router (logic / edge / security / perf / concurrency)"
  default_prompt: "Route this change through the 5-dimension review panel (logic, edge cases, security, performance, concurrency) and return per-dimension findings."
