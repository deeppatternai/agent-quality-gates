interface:
  display_name: "AQG Startup Preflight"
  short_description: "Session-start repo and GitHub blocker check"
  default_prompt: "Run the project startup preflight from the current git root and report blockers plus relevant advisories before making changes."
